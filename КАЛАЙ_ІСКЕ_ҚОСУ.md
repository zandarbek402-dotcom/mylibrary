# ConMat Transport - Проектті қалай көруге болады

## 🚀 Жылдам бастау (3 қадам)

### 1️⃣ Алғашқы орнату

`setup_project.bat` файлын екі рет басыңыз. Бұл:
- Виртуалды ортаны құрады
- Барлық тәуелділіктерді орнатады
- Дерекқор миграцияларын орындайды

### 2️⃣ Суперпайдаланушыны құру

Терминалда:
```powershell
cd backend
python manage.py createsuperuser
```
Пайдаланушы аты, email және құпия сөзді енгізіңіз.

### 3️⃣ Серверлерді іске қосу

**Backend (бір терминалда):**
- `start_backend.bat` файлын екі рет басыңыз
- Немесе терминалда: `cd backend && python manage.py runserver`

**Frontend (басқа терминалда):**
- `start_frontend.bat` файлын екі рет басыңыз  
- Немесе терминалда: `cd frontend && npm start`

### 4️⃣ Браузерде ашу

- **Frontend**: http://localhost:3000
- **Backend API**: http://localhost:8000
- **Admin Panel**: http://localhost:8000/admin

---

## 📋 Алғашқы қадамдар

1. **PostgreSQL серверін іске қосыңыз**
   - PostgreSQL орнатылған және іске қосылған болуы керек
   - Дерекқорды құрыңыз: `CREATE DATABASE conmat_transport;`

2. **`.env` файлын тексеріңіз**
   - `.env` файлында дерекқор параметрлері дұрыс орнатылғанын тексеріңіз

3. **Тіркелу**
   - Frontend-те `/register` бетіне барып, жаңа пайдаланушы тіркеңіз
   - Немесе Admin панелінде пайдаланушы құрыңыз

---

## 🐳 Docker арқылы (қарапайым)

Егер Docker орнатылған болса:

```powershell
docker-compose up -d
```

Миграциялар:
```powershell
docker-compose exec backend python manage.py migrate
docker-compose exec backend python manage.py createsuperuser
```

---

## ⚠️ Мәселелерді шешу

### PostgreSQL қосылу қатесі
- PostgreSQL серверінің іске қосылғанын тексеріңіз
- `.env` файлындағы параметрлерді тексеріңіз

### Port бос емес
- Басқа портты пайдаланыңыз: `python manage.py runserver 8001`
- Немесе портты пайдаланатын басқа қосымшаны тоқтатыңыз

### Module not found
- Виртуалды ортаның іске қосылғанын тексеріңіз
- `pip install -r requirements.txt` командасын қайта орындаңыз

---

## 📚 Қосымша ақпарат

- Толық нұсқаулар: `SETUP.md`
- API документациясы: `README.md`
- Жоба қорытындысы: `PROJECT_SUMMARY.md`

